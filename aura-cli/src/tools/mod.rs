//! Developer tooling behind the CLI subcommands: formatter (`aura fmt`),
//! linter (`aura lint`), documentation generator (`aura doc`), and the
//! interactive REPL (`aura repl`).

pub mod doc;
pub mod fmt;
pub mod lint;
pub mod repl;
