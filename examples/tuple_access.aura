class Program {
    static void Main() {
        (int, int) point = (10, 20);
        print(point);
        println();

        int x = point.0;
        int y = point.1;
        print(x);
        print(" ");
        print(y);
        println();
    }
}
